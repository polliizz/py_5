num = random.randint(1, 10)
tries = 3
while tries > 0:
    guess = int(input("Введіть число від 1 до 10: "))
    if guess == num:
        print("Правильно")
        break
    tries -= 1
    print(f"Ні.Спроб: {tries}")
if tries == 0:
    print(f"Загадане число: {num}")
